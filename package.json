{
  "name": "obnofi-crawler",
  "version": "1.0.0",
  "description": "Crawling microservice that fetches a URL (including JS-rendered SPAs) and returns clean Markdown.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "install:browser": "playwright install chromium"
  },
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1",
    "playwright": "^1.47.0",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2"
  },
  "devDependencies": {
    "pino-pretty": "^11.2.2"
  }
}
